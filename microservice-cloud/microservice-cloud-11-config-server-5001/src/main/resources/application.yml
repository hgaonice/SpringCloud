server:
  port: 5001
spring:
  application:
    name: microservice-config

  cloud:
    config:
      server:
        git:
          uri: https://github.com/GitSorcerer/microservice-cloud-config.git

